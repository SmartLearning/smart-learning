{
  "name": "smart-learning",
  "scripts": {
    "postdeploy": "java -version && curl -o- -L https://yarnpkg.com/install.sh | bash && export PATH=$HOME/.yarn/bin:$PATH && yarn global add bower gulp-cli gulp"
  },
  "env": {
    "APPLICATION_SECURITY_AUTHENTICATION_JWT_SECRET": {
      "required": true,
      "generator": "secret"
    },
    "APP_DIR": {
      "required": true,
      "value": "app"
    },
    "GRADLE_USER_HOME": {
      "required": true
    },
    "LIBRATO_PASSWORD": {
      "required": true
    },
    "LIBRATO_TOKEN": {
      "required": true
    },
    "LIBRATO_USER": {
      "required": true
    },
    "MONGODB_URI": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [
    "librato",
    "logentries",
    {
      "plan": "mongolab",
      "as": "MONGODB"
    }
  ],
  "buildpacks": [
    {
      "url": "heroku/gradle"
    },
    {
      "url": "heroku/java"
    },
    {
      "url": "heroku/nodejs"
    }
  ]
}
